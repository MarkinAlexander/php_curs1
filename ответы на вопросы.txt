ќбъ€снить, как работает данный код:
<?php
    $a = 5; 
    $b = '05';
    var_dump($a == $b);         // ѕочему true?
	//истина так как сравниваютс€ значени€ и ноль отбрасываетс€ и получаетс€ в одной переменной 5 и в другой 5
    var_dump((int)'012345');     // ѕочему 12345?
	//приводит к €вному типу инт в котором ноль отбрасываетс€
    var_dump((float)123.0 === (int)123.0); // ѕочему false?
	//ложь так как при === идет сравнение типов, и в данном случае целочисленое с числом с плавающей точкой
    var_dump((int)0 === (int)'hello, world'); // ѕочему true?
	/* подозрвею что всЄ тоже самое что и выше, так как мы €вно пытаемс€ приравн€ть одну и другю переменную к целому числу и сравнивать по типу переменной, 
поэтому и получаетс€ истина */
?>

5. *»спользу€ только две переменные, помен€йте их значение местами. Ќапример, если a = 1, b = 2, надо, чтобы получилось: b = 1, a = 2. ƒополнительные переменные использовать нельз€.


<?
//объ€вл€ем переменные
$a=1;
$b=2;
//необходимо помен€ть местами что бы а=2 а b=1
$a=$a+$b; // a=2+1=3
$b=$a-$b; //b=3-1=2 то есть сейчас мы достигли нужного результата дл€ b
$a=$a-$b; //a=3-2=1 так как в b сейчас другое значение, произвед€ операцию что и выше мы получим правильное решение
?>